# نقشهٔ راه فورک — «OmniGlyph ما» + یکپارچه‌سازی OmniRoute

برنامهٔ کاری تلفیقی (2026-07-05) برگرفته از: بررسی دقیق صورت‌حساب، حسابرسی
OpenAI/Gemini در برابر مستندات رسمی، تحلیل ابزارهای مرتبط، و مجموعهٔ
density-frontier. وضعیت هر مورد: ☐ در انتظار · ◐ جزئی · ☑ در این مخزن انجام‌شده.

## فاز ۰ — بنیان اندازه‌گیری (در این مخزن انجام‌شده)

- ☑ صورت‌حساب دقیق Anthropic (پچ‌های ۲۸ پیکسلی، ۲ لایه، +۴/بلاک) —
  `src/core/anthropic-vision.ts`، بررسی در `benchmarks/billing-sweep/`.
- ☑ گیت سودآوری با هزینهٔ دقیق (جایگزین w·h/750 × 1.10).
- ☑ هندسه به‌ازای هر لایه: Fable/Opus 4.8/Sonnet 5 → صفحات 1928×1928 (۳.۳
  برابر تصویر کمتر)؛ استاندارد → 1568×728. ۶۹۱ تست سبز.
- ☑ مجموعهٔ `benchmarks/density-frontier/` (هزینه × دقت آفلاین از طریق API،
  needleهای دارای distractorهای گیج‌کننده، امتیازدهی قطعی).

## فاز ۱ — رفع اشکالات صورت‌حساب چندارائه‌دهنده (باگ‌های تأییدشده در حسابرسی)

اولویت تعیین‌شده توسط حسابرسی (مستندات رسمی ثبت‌شده در 2026-07-05):

1. ☐ **D2 (گیت معکوس)**: `gpt-4o-mini` در تایل پیش‌فرض 85/170 قرار می‌گیرد،
   اما **2833 پایه / 5667 به‌ازای هر تایل** هزینه دارد (حدود ۳۳ برابر
   کم‌تخمین، حدود ۰.۸ کاراکتر بر توکن) — تصویرسازی روی آن همیشه یک زیان است
   و گیت آن را تأیید می‌کند. `src/core/gpt-model-profiles.ts:51-59`.
2. ☐ **D5**: `detail:'original'` بدون شرط ارسال می‌شود
   (`src/core/openai.ts:392,402`)، اما فقط در gpt-5.4+ وجود دارد؛ آن را از
   پروفایل استخراج کنید.
3. ☐ **D1**: ضریب `o4-mini` باید از 1.62 → **1.72** باشد (۵.۸٪ کم‌تخمین).
4. ☐ **D3/D4**: `gpt-5.2/5.2-codex/5.3-codex/5.1-codex-mini/5-codex-mini` در
   سبد پچ **سقف 1536 بدون `original`** قرار دارند (کد فرض می‌کند 10000)؛
   `gpt-5-codex-mini` در رژیم اشتباه است (تایل → پچ).
5. ☐ **هندسهٔ GPT**: `GPT_MAX_HEIGHT_PX 1932 → 2048` (با هر دو رژیم هم‌راستا
   می‌شود: پچ ۶۴×۳۲ و تایل ۴×۵۱۲؛ +۶.۲۵٪ کاراکتر رایگان). پروفایل اختصاصی
   `original` برای 5.4/5.5: تا 1568×5984 (9,163 پچ ≤ 10k، حدود ۲۳۳ هزار
   کاراکتر در یک بلاک) — ابتدا A/B خوانایی.
6. ☐ **پشتیبانی از Gemini** (جدید): `src/core/gemini.ts` +
   `gemini-model-profiles.ts` + مسیرهای `:generateContent`/`:streamGenerateContent`
   در پراکسی. هندسهٔ قابل‌مستندسازی: **1152×1536 (واحد برش دقیق 768، ۴ تایل،
   ۴۲.۲ کاراکتر بر توکن — بهترین نسبت مستندشده در میان ۳ ارائه‌دهنده)**؛
   شرط‌بندی‌ها برای کالیبراسیون: 768² با `media_resolution:MEDIUM` (56.4) و
   Gemini 3 HIGH. احتیاط: نقطهٔ پایانی سازگار با OpenAI در Gemini از
   ترنسفورمر OpenAI با صورت‌حساب اشتباه عبور می‌کند.

## فاز ۲ — کیفیت خواندن (مجموعهٔ density-frontier به‌عنوان داور)

- ◐ A/B قطعی استاندارد در برابر وضوح‌بالا روی Fable (در حال اجرا؛ معیار:
  gist == متن **و** صفر خطای بی‌صدا **و** صرفه‌جویی > ۰).
- ☐ حل تناقض AA در برابر ۱-بیت در مسیر فشرده (کد می‌گوید «فقط برای
  ارزیابی»، تولید از AA استفاده می‌کند).
- ☐ (به تعویق افتاده با توجیه در 2026-07-06) جراحی گلیف: پیکربندی تولید
  30/30 می‌خواند — هیچ خطای قابل‌اندازه‌گیری‌ای برای اصلاح توسط جراحی امروز
  وجود ندارد. اگر یک هدف زیر ۱۰۰٪ وارد دامنه شود (مثلاً Opus) یا
  اندازه‌گیری‌های جدید افت نشان دهند، بازبینی شود.
- ☑ ~~A/B تم روشن~~ با بازرسی حل شد (2026-07-06): رندر ALREADY IS
  سیاه‌روی‌سفید است (render.ts:635/822، معکوس‌سازی post-blit) — با ادبیات
  هم‌راستا؛ فرضیه از یک مقدمهٔ اشتباه زاده شده بود (تصویر نمونهٔ upstream).
- ☐ فهرست واژگان با checksum برای شناسه‌های بایت-دقیق (upstream #38، تأییدشده)
  + بنر امتناع (#31/#32) + camelCase در factsheet (#33/#34).
- ☑ پورت #45: $schema/$id حفظ شد، تاپل‌ها به‌ازای هر عنصر حذف شدند
  (کامیت روی main).
- ☑ تلاش مجدد در برابر امتناع (#37/H11): sniffer replay بدون اُفت + یک
  تلاش مجدد با بدنهٔ اصلی؛ تله‌متری refusalRetried (کامیت روی main).
- ☐ ابزار احیا (`RecoverableBlock` → ابزار قابل‌فراخوانی؛ LensVLM
  گسترش مجدد انتخابی را اعتبارسنجی می‌کند).

## فاز ۳ — عملکرد/استحکام

- ☐ کش رندر LRU (قطعی بر اساس ثابت؛ اسلب + بخش‌های منجمد امروز در هر
  درخواست دوباره رندر می‌شوند).
- ☐ رمزگذاری PNG در یک worker thread؛ سطح deflate قابل‌پیکربندی.
- ☐ پورت رفع‌اشکالات باز upstream: #44 (ابزارهای بومی تایپ‌شده → 400)، #45
  (حلقهٔ strip اسکیمای draft-07 → 400)، #42 (پراکسی CONNECT برای Claude
  Desktop)، #19 (صورت‌حساب دوگانهٔ توضیحات GPT).
- ☐ پیاده‌سازی ADAPTIVE_CPT_PLAN (cpt به‌ازای هر نقش بلاک؛ اسلب واقعی = 1.50).

## فاز ۴ — خود فورک

- ☐ نام/مخزن اختصاصی (تصمیم Diego) + `git remote` بالادستی برای cherry-pick.
- ☐ **TS در همه‌جا**: هسته از قبل TS است، `eval/*.mjs`، `demo/`،
  `scripts/*.mjs`، `bench/` را تبدیل کنید (الگو: tsx + vitest؛
  `benchmarks/density-frontier/` از همان ابتدا این‌طور متولد شد).
- ☐ استاندارد کیفیت OmniRoute: eslint 9 + prettier، CI با
  typecheck/test/build/link-check، CONTRIBUTING، SECURITY، README i18n
  (ابتدا pt-BR)، CHANGELOG معنایی.
- ☐ **گیف به‌جای ویدیو** در README (ضبط با vhs/asciinema+agg؛ ساده در
  برابر پراکسی کنار هم).
- ☐ داشبورد v2 (پیاده‌سازی مجدد از طریق HTTP API — کد شخص ثالث را به ارث
  نبرید): لانچر «ترمینال با ANTHROPIC_BASE_URL باز کن»، بررسی «آیا ترافیک
  از من عبور می‌کند؟»، بازرس تصویر در برابر متن، نشست‌ها، پنل هزینه به ارز
  رایج، i18n سبک، SSE به‌جای polling، ماندگاری SQLite با نگه‌داری (اسکیمای
  ۲۴ ستونی آن نقطهٔ شروع خوبی است).
- ☐ ایده‌های خرد از dense-image-gen: حالت `lines` (چیدمان حفظ‌شده برای
  کد/جدول)، `--keep-ws`، عنوان مبدأ به‌ازای هر صفحه («system prompt» /
  «tool docs» / «history turn N»)، CLI مستقل `render arquivo.md -o out.png`.

## فاز ۵ — پورت به OmniRoute

- ☐ موتور `CompressionEngine` (قالب `cavemanAdapter.ts`)، ثبت‌شده در
  `engines/index.ts` + `engineCatalog.ts`؛ `targets: ["messages","tool_results"]`،
  `applyAsync`.
- ☐ سیم‌کشی: عبور `supportsVision` در `chatCore.ts:1297` (۱ خط) یا حل از
  طریق `isVisionModelId`.
- ☐ ترتیب پشته: آخرین (رندرهای معنایی RTK/Caveman ابتدا؛ OmniGlyph مابقی
  را تصویر می‌کند).
- ☐ ثابت‌ها: هرگز بلاک‌های `cache_control` کلاینت را بازنویسی نکنید (درس
  #4560)؛ گیت وفاداری (#5127) به یک معافیت اعلام‌شده یا یک factsheet متنی
  که ثابت‌ها را برآورده می‌کند نیاز دارد؛ تله‌متری تلاش با `skip_reason`
  (درس #4268).
- ☐ مسیریابی: fallback/retry پس از موتور باید قابلیت بینایی و allowlist را
  رعایت کند (فشرده‌سازی مجدد یا bypass).
- ☐ هم‌افزایی CCR: `emitRecoverable` → فروشگاه CCR با بازیابی به‌ازای هر
  برش (`head/tail/grep`، #5187) = گسترش مجدد انتخابی کامل.
- ☐ کشش لایهٔ رایگان به‌عنوان یک ویژگی بازاریابی: هر توکن لایهٔ رایگان
  حدود ۲ تا ۳ برابر کاراکتر بیشتر روی مدل‌های بینایی می‌دهد؛ لایهٔ رایگان
  Gemini + هندسهٔ 1152×1536 قوی‌ترین مورد است.

## ریسک‌های باز

- امتناع Fable پس از استقرار مجدد در بستر تصویرشده (upstream #37) — پیش از
  فعال‌سازی پیش‌فرض در OmniRoute کاهش دهید.
- آربیتراژ قیمت: اگر Anthropic قیمت بینایی را تغییر دهد، صرفه‌جویی‌ها تغییر
  می‌کنند — ضدواقعیت به‌ازای هر درخواست (`count_tokens`) خط دفاعی است.
- OpenAI: اندازه‌گیری انجمنی (PageWatch) افزایش توکن‌های تکمیل و تأخیر ۲
  برابر دید — پیش از فعال‌سازی به‌ازای هر ارائه‌دهنده اندازه‌گیری کنید.

## نتایج A/B در 2026-07-05 (از طریق OpenRouter — برای هندسه غیرقطعی، برای حالت‌های خطا معتبر)

| پیکربندی | verbatim | امتناع | فیلترشده | خطای بی‌صدا |
|---|---|---|---|---|
| fable استاندارد 5×8 (AA و ۱-بیت) | 0/30 | 0 | **30/30 content_filter** | 0 |
| fable وضوح‌بالا 5×8 (AA) | 0/30 | **20 ILEGIVEL** | 0 | 5 (2 پیش‌بینی‌شده) |
| fable وضوح‌بالا 5×8 (۱-بیت) | 0/30 | 20 | 1 | 4 (2 پیش‌بینی‌شده) |
| opus وضوح‌بالا 10×16 | **7/9 خواندن** | 0 | 21 پایان اعتبار | 2 (رقم) |

یافته‌های معتبر: (۱) طبقه‌بند (issue #37) حالت خطای غالب برای پرسش‌های
رونویسی روی صفحهٔ استاندارد است — ۱۰۰٪ فیلترشده — و روی صفحهٔ بزرگ فعال
نمی‌شود؛ عبارت‌بندی اهمیت دارد. (۲) امتناع کار می‌کند: ۲۰ برابر ILEGIVEL در
برابر ۵ ساختهٔ خیالی روی صفحهٔ بزرگ. (۳) Opus در 10×16 حدود ۷۸٪ دقیق
می‌خواند (n=9) در برابر ۰٪ تاریخی در 5×8 — اولین شاهد مستقیم از زانوی
منحنی (knee). (۴) ناخوانا بودن صفحهٔ بزرگ از طریق OpenRouter نشان‌دهندهٔ
یک RESAMPLE در لایهٔ انتقال است (Bedrock/Vertex لایهٔ استاندارد؟) — فرضیهٔ
قطعی برای آزمون روی API مستقیم Anthropic؛ A/B هندسه تا آن زمان باز
می‌ماند. اعتبار OpenRouter در میانهٔ بازوی Opus تمام شد.

## ماتریس نهایی ۲×۲ (2026-07-05، از طریق CLI/اشتراک، Fable 5، n=30/بازو)

| صفحه × اطلس | ۱-بیت | AA |
|---|---|---|
| استاندارد 1568×728 | **30/30 (100%)** | 25/30 + 5 امتناع |
| وضوح‌بالا 1928×1928 | **20/30 (67%)** + 10 امتناع | 0/30 + 29 امتناع |

صفر ساختهٔ خیالی در هر ۴ بازو (۱۲۰ پرسش — هر خطا ILEGIVEL بود). اعمال‌شده:
DENSE_RENDER_STYLE به ۱-بیت (aa:false) تغییر یافت با یک پین در
tests/dense-style.test.ts. Opus 4.8: 26/30 در 10×16 روی صفحهٔ بزرگ، 30/30
ILEGIVEL در 5×8 — حالت ایمن Opus قابل‌اجراست. صفحهٔ وضوح‌بالا همچنان توسط
لایه‌های انتقال تخریب می‌شود (Read در CLI/تغییر اندازهٔ OpenRouter)؛
حکم هندسهٔ WYSIWYG همچنان به API مستقیم بستگی دارد.
